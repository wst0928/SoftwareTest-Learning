# 功能测试面试题学习

## 1. XX功能是怎么测试的？

### 标准回答框架（6大维度）

#### 1. 入口
- 遍历所有页面入口，看能否正常进入（比如：菜单栏、按钮、快捷键）

#### 2. 权限
- **有权限**：正常操作
- **无权限**：操作时是否被拦截，提示是否友好

#### 3. 业务逻辑（状态机流转）
- 正向流程（如：登录成功跳转首页）
- 逆向流程（如：已发货的订单能否退款？）
- 异常分支（如：支付超时后订单状态变化）

#### 4. 字段校验
- 长度（如：用户名最大20位）
- 类型（如：手机号只能输数字）
- 是否必填（为空时拦截）
- 是否唯一（重复时提示）

#### 5. 容错性/异常场景
- 查询结果为空（展示空状态页）
- 断网时的反馈（提示网络异常）
- 多次快速点击（防重放）

#### 6. 安全性
- 接口是否存在越权（未登录能否访问）

---

## 2. XX模块是怎么测试的？

### 解析
> 相当于问：“这个模块拥有哪些功能？”

### 回答思路
1. 先列出该模块包含的所有功能点（基于需求文档）
2. 针对每个功能点，套用上面 **“6大维度”** 逐个分析
3. 引导话术：“我们的测试点分析，是基于需求和功能点来拆解的。”

---

## 3. 给你一个购物车/微信发红包功能是怎么测试的？

###UI 功能 兼容 易用性 

## 4.偶发性bug

### 第1步：详细记录现场信息
- 设备品牌型号、操作系统版本、App/浏览器版本
- 网络环境（WiFi/4G/5G/弱网）
- 详细操作步骤 + 截图/录屏
- 出错时段的**完整日志**（logcat/控制台报错）

### 第2步：抓包分析
- 用Charles/Fiddler抓取出错时的**接口请求和响应**
- 对比正常和异常时的数据差异
- 区分是**前端渲染问题**还是**后端数据异常**

### 第3步：尝试复现
- 通过**探索性测试**，大量多次操作
- 寻找规律（是否和特定操作速度、特定数据有关）
- 记录复现概率（比如“操作20次出现1次”）

### 第4步：线上止血（如有影响）
- 如果影响面大，和产品沟通**临时规避方案**
- 如：隐藏入口、降级提示、热修复

### 第5步：协助开发定位
- 提供日志、抓包数据、复现步骤
- 讨论分析可能原因（并发？内存？时序？）
- 持续监控线上是否还有类似报错

### 第6步：上线后持续跟进
- 修复后，后续**连续几个迭代**都要做回归测试
- 确保问题真正根治，而不是碰巧没出现

---

## 面试话术总结

> “对于任何一个功能，我都会从**入口、权限、业务状态、字段校验、异常容错、安全越权**这六个维度进行全方面覆盖，确保不遗漏任何质量问题。”
